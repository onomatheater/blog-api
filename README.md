# 📝 Blog API - Полнофункциональный блог с REST API

Веб-приложение блога с JWT авторизацией, CRUD операциями, комментариями и Redis кэшированием.

## 🎯 Возможности

- ✅ **Регистрация и авторизация** с JWT токенами
- ✅ **CRUD операции для публикаций** (создание, чтение, обновление, удаление)
- ✅ **CRUD операции для комментариев**
- ✅ **Проверка прав владельца** - редактирование/удаление только своего контента
- ✅ **Redis кэширование** для оптимизации производительности
- ✅ **Swagger UI документация** API
- ✅ **Веб-интерфейс** для удобной работы с блогом
- ✅ **Docker контейнеризация** для простого развёртывания
- ✅ **Adminer** для управления базой данных

***

## 🛠 Стек технологий

| Компонент | Технология |
| :-- | :-- |
| **Backend** | Python 3.11, FastAPI |
| **Database** | PostgreSQL 15 |
| **Cache** | Redis 7 |
| **Auth** | JWT (JSON Web Tokens) |
| **DB Admin** | Adminer |
| **API Docs** | Swagger UI, ReDoc |
| **Frontend** | Vanilla JavaScript, HTML5, CSS3 |
| **Deployment** | Docker, Docker Compose |


***

## 📋 Требования

Перед началом установите:

### Обязательно:

- **Docker** (версия 20.10+) - [Инструкция по установке](https://docs.docker.com/get-docker/)
- **Docker Compose** (версия 2.0+) - [Инструкция по установке](https://docs.docker.com/compose/install/)


### Проверка установки:

```bash
docker --version
# Docker version 24.0.0 или выше

docker-compose --version
# Docker Compose version v2.20.0 или выше
```


***

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Клонирование проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/onomatheater/blog-api.git
cd blog-api

# Или создайте структуру вручную
mkdir blog-api
cd blog-api
```


### Шаг 2: Создание файла окружения

Создайте файл `.env` в корне проекта:

```bash
# Database Configuration
POSTGRES_USER=bloguser
POSTGRES_PASSWORD=blogpass123
POSTGRES_DB=blogdb
DATABASE_URL=postgresql://bloguser:blogpass123@db:5432/blogdb

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Application Configuration
APP_HOST=0.0.0.0
APP_PORT=8000
```

⚠️ **ВАЖНО**: В продакшене обязательно измените `SECRET_KEY`!

**Генерация безопасного ключа:**

```bash
# Linux/Mac
openssl rand -hex 32

# Python (любая ОС)
python -c "import secrets; print(secrets.token_urlsafe(32))"
```


### Шаг 3: Запуск приложения

```bash
# Запуск всех сервисов
docker-compose up --build
```

**Что происходит:**

- 📦 Docker скачивает образы (PostgreSQL, Redis, Adminer)
- 🔨 Собирает ваше приложение
- 🚀 Запускает все контейнеры
- ⏱️ Первый запуск: ~2-5 минут


### Шаг 4: Проверка работы

Откройте в браузере:


| Сервис | URL | Описание |
| :-- | :-- | :-- |
| **Веб-интерфейс** | http://localhost:8000 | Главная страница блога |
| **Swagger Docs** | http://localhost:8000/docs | Интерактивная документация API |
| **ReDoc** | http://localhost:8000/redoc | Альтернативная документация |
| **Adminer** | http://localhost:8080 | Управление базой данных |

**Для доступа в Adminer:**

```
Система:         PostgreSQL
Сервер:          db
Пользователь:    bloguser
Пароль:          blogpass123
База данных:     blogdb
```


***

## 📱 Использование

### Веб-интерфейс

1. Откройте http://localhost:8000
2. Нажмите **"Регистрация"**
3. Заполните форму:
    - Имя пользователя
    - Email
    - Пароль
4. Нажмите **"Войти"** и введите данные
5. Создавайте публикации и комментируйте!

### Через Swagger UI

1. Откройте http://localhost:8000/api/v1/docs
2. Разверните раздел **POST /api/v1/auth/register**
3. Нажмите **"Try it out"**
4. Введите данные и нажмите **"Execute"**
5. Для защищённых эндпоинтов:
    - Нажмите **"Authorize"** вверху
    - Введите токен в формате: `Bearer <ваш_токен>`

***

## 🔌 API Endpoints

### 🔐 Аутентификация

#### Регистрация

```http
POST /api/v1/auth/register
Content-Type: application/json

{
  "username": "john_doe",
  "email": "john@example.com",
  "password": "securepass123"
}
```


#### Вход (получение JWT токена)

```http
POST /api/v1/auth/login
Content-Type: application/x-www-form-urlencoded

username=john@example.com&password=securepass123
```

**Ответ:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```


***

### 📝 Публикации

#### Получить все публикации

```http
GET /api/v1/posts
Authorization: Bearer <token>
```


#### Получить публикацию по ID

```http
GET /api/v1/posts/{post_id}
Authorization: Bearer <token>
```


#### Создать публикацию

```http
POST /api/v1/posts
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Моя первая публикация",
  "content": "Содержание публикации..."
}
```


#### Обновить публикацию (только владелец)

```http
PUT /api/v1/posts/{post_id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Обновлённый заголовок",
  "content": "Новое содержание..."
}
```


#### Удалить публикацию (только владелец)

```http
DELETE /api/v1/posts/{post_id}
Authorization: Bearer <token>
```


***

### 💬 Комментарии

#### Получить комментарии к публикации

```http
GET /api/v1/posts/{post_id}/comments
Authorization: Bearer <token>
```


#### Добавить комментарий

```http
POST /api/v1/posts/{post_id}/comments
Authorization: Bearer <token>
Content-Type: application/json

{
  "content": "Отличная статья!"
}
```


#### Обновить комментарий (только владелец)

```http
PUT /api/v1/posts/{post_id}/comments/{comment_id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "content": "Обновлённый комментарий"
}
```


#### Удалить комментарий (только владелец)

```http
DELETE /api/v1/posts/{post_id}/comments/{comment_id}
Authorization: Bearer <token>
```


***

## 🧪 Примеры использования через curl

### 1. Регистрация пользователя

```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "password123"
  }'



### 2. Вход (получение токена)

```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=test@example.com&password=password123"
```

**Сохраните токен из ответа!**

```bash
TOKEN="ВАШ_ТОКЕН_ИЗ_ОТВЕТА"
```

### 3. Создание публикации

```bash
curl -X POST "http://localhost:8000/api/v1/posts" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Моя первая публикация",
    "content": "Это содержание моей публикации"
  }'
```


### 4. Получение всех публикаций

```bash
curl -X GET "http://localhost:8000/api/v1/posts" \
  -H "Authorization: Bearer $TOKEN"
```


### 5. Добавление комментария

```bash
curl -X POST "http://localhost:8000/api/v1/posts/1/comments" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Отличная публикация!"
  }'
```


***

## 🔧 Управление приложением

### Запуск в фоновом режиме

```bash
docker-compose up -d
```


### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только база данных
docker-compose logs -f db

# Последние 50 строк
docker-compose logs --tail=50 backend
```


### Остановка приложения

```bash
# Остановить контейнеры
docker-compose down

# Остановить + удалить данные (БД, кэш)
docker-compose down -v
```


### Перезапуск сервиса

```bash
# Перезапустить backend
docker-compose restart backend

# Перезапустить все сервисы
docker-compose restart
```


### Пересборка после изменений кода

```bash
docker-compose up --build
```


### Проверка статуса контейнеров

```bash
docker-compose ps
```


***

## 📁 Структура проекта

```
blog-api/
├── app/                          # Код приложения
│   ├── __init__.py
│   ├── main.py                  # Точка входа FastAPI
│   ├── config.py                # Конфигурация (env переменные)
│   ├── database.py              # Подключение к БД
│   │
│   ├── models/                  # SQLAlchemy модели (таблицы БД)
│   │   ├── __init__.py
│   │   ├── user.py             # Модель пользователя
│   │   ├── post.py             # Модель публикации
│   │   └── comment.py          # Модель комментария
│   │
│   ├── schemas/                 # Pydantic схемы (валидация)
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── post.py
│   │   └── comment.py
│   │
│   ├── routers/                 # API маршруты
│   │   ├── __init__.py
│   │   ├── auth.py             # Регистрация/вход
│   │   ├── posts.py            # CRUD публикаций
│   │   └── comments.py         # CRUD комментариев
│   │
│   ├── services/                # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── auth.py             # JWT, хэширование
│   │   └── cache.py            # Redis кэширование
│   │
│   └── utils/                   # Вспомогательные функции
│       ├── __init__.py
│       └── security.py          # Проверка прав
│
├── frontend/                     # Веб-интерфейс
│   └── index.html               # SPA приложение
│
├── tests/                        # Тесты (pytest)
│   ├── __init__.py
│   ├── test_auth.py
│   ├── test_posts.py
│   └── test_comments.py
│
├── .env                          # Переменные окружения (НЕ в git!)
├── .gitignore                    # Игнорируемые файлы
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Docker образ приложения
├── requirements.txt              # Python зависимости
└── README.md                     # Документация (этот файл)
```


***

## 🐛 Решение проблем

### Проблема: Порты уже заняты

**Симптом:** `Error: bind: address already in use`

**Решение:** Измените порты в `docker-compose.yml`:

```yaml
services:
  backend:
    ports:
      - "8001:8000"  # Изменили с 8000 на 8001
  
  db:
    ports:
      - "5433:5432"  # Изменили с 5432 на 5433
```


### Проблема: База данных не инициализируется

**Симптом:** Ошибки подключения к БД

**Решение:**

```bash
# Полная очистка и перезапуск
docker-compose down -v
docker-compose up --build
```


### Проблема: Ошибки импорта Python

**Симптом:** `ModuleNotFoundError`

**Решение:**

```bash
# Пересборка без кэша
docker-compose build --no-cache backend
docker-compose up
```


### Проблема: Frontend не подключается к API

**Симптом:** CORS ошибки в консоли браузера

**Решение:** Проверьте в `app/main.py`:

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # В продакшене укажите конкретные домены
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```


### Проблема: Redis недоступен

**Симптом:** Ошибки подключения к Redis

**Решение:**

```bash
# Перезапустить Redis
docker-compose restart redis

# Проверить логи
docker-compose logs redis
```


### Полная очистка Docker

Если ничего не помогает:

```bash
# Остановить все контейнеры
docker-compose down -v

# Удалить все образы проекта
docker system prune -a

# Заново собрать и запустить
docker-compose up --build
```


***

## 🔒 Безопасность

### Реализовано:

- ✅ **Хэширование паролей** с помощью bcrypt
- ✅ **JWT токены** для безопасной аутентификации
- ✅ **Валидация данных** через Pydantic
- ✅ **Проверка прав владельца** на операции изменения/удаления
- ✅ **SQL инъекции** предотвращены через SQLAlchemy ORM
- ✅ **XSS защита** - экранирование данных во frontend


### Для продакшена:

⚠️ **Обязательно:**

1. Измените `SECRET_KEY` на криптостойкий ключ (32+ символа)
2. Настройте CORS для конкретных доменов (не `*`)
3. Используйте HTTPS (SSL сертификаты)
4. Ограничьте доступ к Adminer
5. Настройте rate limiting
6. Используйте environment secrets (не .env в репозитории)

**Пример CORS для продакшена:**

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://yourdomain.com",
        "https://www.yourdomain.com"
    ],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)
```


***

## 📈 Производительность

### Кэширование

- **Redis кэш** для списка публикаций
- **TTL**: 5 минут (300 секунд)
- **Инвалидация**: При создании/обновлении/удалении публикации


### Оптимизация БД

- Индексы на часто используемых полях (`email`, `post_id`, `user_id`)
- Connection pooling через SQLAlchemy
- Async операции (опционально)


### Мониторинг

```bash
# Использование ресурсов контейнерами
docker stats

# Размер базы данных (в Adminer)
SELECT pg_size_pretty(pg_database_size('blogdb'));
```


***

## 🔄 Обновление приложения

### При изменении кода:

```bash
# 1. Остановить
docker-compose down

# 2. Получить изменения (если git)
git pull

# 3. Пересобрать и запустить
docker-compose up --build
```


### При изменении зависимостей:

```bash
# Пересборка без кэша
docker-compose build --no-cache
docker-compose up
```


### Миграции БД:

```bash
# Если используете Alembic
docker-compose exec backend alembic upgrade head
```


***

## 🌐 Деплой на сервер

### Подготовка сервера (Ubuntu/Debian):

```bash
# 1. Обновить систему
sudo apt update && sudo apt upgrade -y

# 2. Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# 3. Установить Docker Compose
sudo apt install docker-compose-plugin -y

# 4. Клонировать проект
git clone <ваш-репозиторий>
cd blog-api

# 5. Настроить .env
nano .env
# Измените SECRET_KEY, пароли БД

# 6. Запустить
sudo docker-compose up -d
```


### Nginx (reverse proxy):

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```


### Бесплатный хостинг:

| Сервис | Backend (Python) | Frontend | БД |
| :-- | :-- | :-- | :-- |
| **Render.com** | ✅ Бесплатно (750ч) | ✅ | ✅ PostgreSQL |
| **Railway.app** | ✅ \$5 кредит | ✅ | ✅ |
| **Fly.io** | ✅ Лимиты | ✅ | ✅ |
| **PythonAnywhere** | ✅ Ограничено | ❌ | ✅ MySQL |
| **Vercel** | ❌ | ✅ | ❌ |
| **Netlify** | ❌ | ✅ | ❌ |


***

## 📝 Переменные окружения

| Переменная | Описание | Значение по умолчанию | Обязательна |
| :-- | :-- | :-- | :-- |
| `POSTGRES_USER` | Пользователь PostgreSQL | `bloguser` | ✅ |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | `blogpass123` | ✅ |
| `POSTGRES_DB` | Имя базы данных | `blogdb` | ✅ |
| `DATABASE_URL` | URL подключения к БД | `postgresql://...` | ✅ |
| `SECRET_KEY` | Секретный ключ для JWT | ⚠️ Обязательно измените! | ✅ |
| `ALGORITHM` | Алгоритм JWT | `HS256` | ✅ |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена (мин) | `30` | ✅ |
| `REDIS_URL` | URL подключения к Redis | `redis://redis:6379/0` | ✅ |
| `APP_HOST` | Хост приложения | `0.0.0.0` | ❌ |
| `APP_PORT` | Порт приложения | `8000` | ❌ |


***

## 🧪 Тестирование

### Запуск тестов:

```bash
# Установить pytest
pip install pytest pytest-asyncio httpx

# Запустить тесты
pytest tests/ -v

# С покрытием кода
pytest tests/ --cov=app --cov-report=html
```


### Пример теста:

```python
# tests/test_auth.py
def test_register_user(client):
    response = client.post(
        "/api/v1/auth/register",
        json={
            "username": "testuser",
            "email": "test@example.com",
            "password": "password123"
        }
    )
    assert response.status_code == 201
    assert "id" in response.json()
```


***

## 🤝 Вклад в проект

Если хотите улучшить проект:

1. **Fork** репозитория
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Commit: `git commit -m 'Add amazing feature'`
4. Push: `git push origin feature/amazing-feature`
5. Откройте **Pull Request**

### Код стиль:

- Python: PEP 8, black formatter
- JavaScript: ESLint, Prettier
- Commits: Conventional Commits

***

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Docker документация](https://docs.docker.com/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)
- [Redis документация](https://redis.io/documentation)
- [JWT.io](https://jwt.io/) - информация о JWT токенах

***

## 📄 Лицензия

Этот проект создан в образовательных целях.

***

## 📞 Поддержка

При возникновении проблем:

1. Проверьте раздел [Решение проблем](#-%D1%80%D0%B5%D1%88%D0%B5%D0%BD%D0%B8%D0%B5-%D0%BF%D1%80%D0%BE%D0%B1%D0%BB%D0%B5%D0%BC)
2. Посмотрите логи: `docker-compose logs backend`
3. Создайте Issue в репозитории с описанием проблемы

***

## ✅ Чеклист для проверки работы

- [ ] Docker и Docker Compose установлены
- [ ] Файл `.env` создан и настроен
- [ ] Команда `docker-compose up --build` выполнена успешно
- [ ] http://localhost:8000 открывается
- [ ] Регистрация нового пользователя работает
- [ ] Вход в систему работает
- [ ] Создание публикации работает
- [ ] Добавление комментария работает
- [ ] http://localhost:8000/docs показывает Swagger
- [ ] http://localhost:8080 открывает Adminer
- [ ] База данных содержит таблицы: users, posts, comments

***

**Готово! 🎉 Ваш блог запущен и работает!**

Сделано с ❤️ для изучения FastAPI, Docker и современной веб-разработки

